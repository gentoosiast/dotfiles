-- TODO: find better way to define plugin mappings, so they don't
-- produce errors on bootstrap

-- hop
--vim.keymap.set('n', "<Leader>'", require'hop'.hint_char1)

-- nvim-tree
--vim.keymap.set('n', '<Leader>e', require'nvim-tree'.toggle)

